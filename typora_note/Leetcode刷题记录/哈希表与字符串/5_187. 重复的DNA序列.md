<img src="5_187. 重复的DNA序列.assets/image-20201130164928275.png" alt="image-20201130164928275" style="zoom:33%;" />

思路：就是用一个长度为10的框子沿着整个字符串移动，如果遇到了重复的字符串，就将其add到List中

```java
class Solution {
    public List<String> findRepeatedDnaSequences(String s) {
         List<String> res=new ArrayList<>();
        HashMap<String,Integer> mapTemp=new HashMap<>();
        //这次开始真 滑动窗口
        // System.out.println(s.length());
        if(s.length()<10)
        {
            return res;
        }
        // System.out.println(s.substring(0,10));//[ ）左边不包括
        for(int i=0;i< s.length();i++)
        {
            int left=i;
            int right=i+10;//左右边界
            if(right>s.length())
            {
                break;//如果右边边界>数组长度 就得退出循环
            }
            String temp=s.substring(left,right);
           
            if(mapTemp.containsKey(temp)&&mapTemp.get(temp)==1)
            {
                res.add(temp);
                mapTemp.put(temp,1+1);
//                System.out.println("res:"+res);
            }
            if(!mapTemp.containsKey(temp))
            {
//                System.out.println("不存在的"+temp);
                mapTemp.put(temp,1);
            }
        }
         
         
         
         return res;

    }
}
```

简洁的代码

```java
class Solution {
  public List<String> findRepeatedDnaSequences(String s) {
    int L = 10, n = s.length();
    HashSet<String> seen = new HashSet(), output = new HashSet();

    // iterate over all sequences of length L
    for (int start = 0; start < n - L + 1; ++start) {
      String tmp = s.substring(start, start + L);
      if (seen.contains(tmp)) output.add(tmp);
      seen.add(tmp);
    }
    return new ArrayList<String>(output);
  }
}

```

